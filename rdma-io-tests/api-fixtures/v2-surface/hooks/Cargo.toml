[package]
name = "v2-surface-hook-fixtures"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
rdma-io = { path = "../../../../rdma-io", default-features = false, features = ["tokio", "test-hooks"] }

[workspace]
